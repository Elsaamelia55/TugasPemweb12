   function tampilgambar() {
      const imagePicker = document.getElementById('imagePicker');
      const select = imagePicker.value;
      const tampilan = document.getElementById('tampilan');
      const fotosatu = select + ".jpg";
      const fotodua = select + ".jpg";
      const fototiga = select + ".jpg";
      const fotoempat = select + ".jpg";
      const fotolima = select + ".jpg";
      const fotoenam = select + ".jpg";
  }
  